login(String username, String password)
getUpdate()
getSearchResult(String s) //return a list of places with these data : name, id, city, province, country, type(marker,polygon, polyline)
getSearchItem(int i); //return hole data of a place with it's id (name , ... and it's point(s)) to show on the map
sendUserLocations(List<userLocationModel>); // send a list of userlocations to server 
getAddress(Geopoint p); //get address of a uniqe location (for long click map)
getDirection(Geopoint p1, Geopoint p2); //make a road between 2 point 
getUserOverlays(); //return all user overlays contain polygon, polyline and Markers 
